#include <iostream>
using namespace std;
bool permu(string ch, int count)
{
    int arr[26] = {0};
    int num = 0;
    for (int i = 0; i < arr.length(); i++)
    {
        int num = arr[i] - 'a';
    }
    arr[num]++;
}
int main()
{
}